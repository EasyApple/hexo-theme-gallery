.header {
  margin-top: 2em
  display: flex
  align-items: baseline
  justify-content: space-between

  .blog-title .logo {
    color: #555;
    font-size: 2em;
    font-family: ChaletComprimeMilanSixty;
    text-decoration: none;
  }

  .menu {
    margin: 0;
    padding: 0;

    .menu-item {
      display: inline-block;
      margin-right: 10px;
    }

    .menu-item-link {
      color: #555;
      text-decoration: none;

      &:hover {
        color: #368CCB;
      }
    }
  }
}